- template.name = "Default Blog"
- template.resource = Blog::Blog
- title @blog.title

%h3= @blog.title

- @blog.posts.each do |post|
  .post
    %h4= link_to post.title, post
    - if post.post_image.filename?
      = image post.post_image.thumbnail
    = post.content
    